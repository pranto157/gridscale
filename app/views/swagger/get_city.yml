parameters:
  - in: path
    name: city_uuid
    required: true
    description: The UUID of the City!
    type: string

responses:
  200:
    description: Information about a city
    schema:
      $ref: '#/definitions/City'
